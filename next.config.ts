import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // /config for unsplash image https://bunchhatcollection.com

  images: {
    domains: ["images.unsplash.com", "bunchhatcollection.com"],
    
  },
};

export default nextConfig;
